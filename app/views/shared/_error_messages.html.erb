<% if @job && @job.errors.any? %>
  <div id="error_explanation">
  <h2><%= pluralize(@job.errors.count, "error")%> prohibited this jobs from being saved: </h2>
  <ul>
    <% @job.errors.full_messages.each do |m| %>
      <li><%= m %></li>
    <% end %>
  </ul>
  </div>
<% end %>
